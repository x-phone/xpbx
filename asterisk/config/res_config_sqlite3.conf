;
; res_config_sqlite3 — SQLite3 Realtime driver for Asterisk
; Reads PJSIP endpoints, auths, aors, contacts, and dialplan from SQLite
;
[asterisk]
dbfile = /data/asterisk-realtime.db
busy_timeout = 5000
wal_mode = yes
